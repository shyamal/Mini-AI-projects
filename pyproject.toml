[project]
name = "ai-model-comparator"
version = "0.1.0"
description = "Benchmark and compare AI model outputs across quality metrics"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"
